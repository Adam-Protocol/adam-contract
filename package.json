{
  "name": "adam-contract",
  "version": "1.0.0",
  "description": "Adam Protocol Starknet Contracts",
  "scripts": {
    "deploy": "ts-node scripts/deploy.ts",
    "deploy:sepolia": "ts-node scripts/deploy.ts --network sepolia",
    "deploy:mainnet": "ts-node scripts/deploy.ts --network mainnet"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
