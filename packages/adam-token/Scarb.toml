[package]
name = "adam_token"
version = "0.1.0"
edition = "2024_07"
description = "Adam Protocol ERC-20 token — deployed as ADUSD and ADNGN"

[dependencies]
starknet.workspace = true
openzeppelin.workspace = true
adam_common = { path = "../adam-common" }

[dev-dependencies]
snforge_std.workspace = true

[[target.starknet-contract]]
sierra = true

[scripts]
test = "snforge test"
